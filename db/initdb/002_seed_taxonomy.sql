-- ─────────────────────────────────────────────────────────────────────────────
-- Seed: 002_seed_taxonomy.sql
-- Description: Initial seed data for taxonomy (Stub)
-- ─────────────────────────────────────────────────────────────────────────────

-- This file runs automatically on fresh container startup (docker-entrypoint-initdb.d)
-- To run manually: make db-seed

-- Example:
-- INSERT INTO meta.taxonomy_node (id, level, name, path)
-- VALUES (gen_random_uuid(), 1, 'AI', 'AI')
-- ON CONFLICT DO NOTHING;

-- For now, this is a placeholder.
SELECT 'Seed data applied' as result;
